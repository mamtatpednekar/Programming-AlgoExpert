def transposeMatrix(matrix):
    # Write your code here.
    matrixTranspose = []
    for col in range(len(matrix[0])):
        newRow = []
        for row in range(len(matrix)):
            newRow.append(matrix[row][col])
        matrixTranspose.append(newRow)
    return matrixTranspose
